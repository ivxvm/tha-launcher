export struct Level {
    name: string,
    gravity: int,
    file: string,
    image: image,
}

export struct GalleryImage {
    title: string,
    author: string,
    url: string,
}

export enum MenuTab {
    None, Play, Gallery, Options, About
}
